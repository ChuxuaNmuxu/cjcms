import props from './props';
import propsbar from './propsbar';

/**
 * 默认slide配置
 */
const slide = {
    description: '这是slide', // 描述
    propsbar, // 自定义属性栏
    props, // 属性，支持简单编排和自定义组件
}

export default slide;
